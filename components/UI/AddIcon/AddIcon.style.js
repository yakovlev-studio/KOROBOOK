import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  crossUp: {
    backgroundColor: "#ccc"
  },
  crossFlat: {
    position: "absolute",
    backgroundColor: "#ccc"
  }
});
