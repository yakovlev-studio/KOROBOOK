import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    buttonsWrapper: {
        position: 'absolute', 
        right: 0, 
        bottom: 0, 
        flexDirection: 'row',
        alignItems: 'center'
    }
})

export default styles